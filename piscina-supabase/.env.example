# =====================================================
# CONFIGURAZIONE AMBIENTE - Sistema Gestione Piscina
# =====================================================
# Copia questo file in .env e modifica con i tuoi valori
# =====================================================

# ========== SUPABASE ==========
# URL del progetto Supabase
# Locale: http://localhost:54321
# Production: https://your-project-ref.supabase.co
SUPABASE_URL=http://localhost:54321

# Anon Key (pubblica, safe nel frontend)
# Ottienila da Supabase Studio → Settings → API → anon/public
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role Key (PRIVATA, solo backend/Edge Functions)
# ⚠️ NON committare su Git! Solo per server-side
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ========== EMAIL (BREVO / SENDINBLUE) ==========
# API Key Brevo per invio email transazionali
# Ottienila da: https://app.brevo.com/settings/keys/api
BREVO_API_KEY=xkeysib-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Email mittente
BREVO_SENDER_EMAIL=noreply@piscina.it
BREVO_SENDER_NAME=Sistema Piscina

# ========== FRONTEND ==========
# URL base del frontend (per redirect email)
# Locale: http://localhost:8080
# Production: https://your-site.com
FRONTEND_URL=http://localhost:8080

# ========== CORS ==========
# Origini consentite per CORS (separare con virgola)
CORS_ORIGINS=http://localhost:8080,http://localhost:3000

# ========== PWA ==========
# Abilita notifiche push (true/false)
ENABLE_PUSH_NOTIFICATIONS=false

# VAPID Public Key (per push notifications)
# Genera con: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# ========== STORAGE ==========
# Max dimensione file upload (in MB)
MAX_FILE_SIZE_MB=5

# Formati file consentiti per documenti (separare con virgola)
ALLOWED_FILE_TYPES=pdf,jpg,jpeg,png

# ========== CONFIGURAZIONI BUSINESS ==========
# Giorni validità pacchetti (se non specificato nel pacchetto)
DEFAULT_PACKAGE_VALIDITY_DAYS=180

# Ore per controllo doppio check-in
DOUBLE_CHECKIN_HOURS=4

# Ore fascia oraria (mattina/pomeriggio)
# Mattina: 00:00 - FASCIA_ORARIA_THRESHOLD
# Pomeriggio: FASCIA_ORARIA_THRESHOLD - 23:59
FASCIA_ORARIA_THRESHOLD=14

# Giorni reminder prenotazione
REMINDER_DAYS_BEFORE=1

# ========== LOGGING ==========
# Livello log (debug, info, warn, error)
LOG_LEVEL=info

# Abilita activity log (true/false)
ENABLE_ACTIVITY_LOG=true

# ========== DEVELOPMENT ==========
# Ambiente (development, production)
NODE_ENV=development

# Porta server locale (se necessario)
PORT=3000

# ========== SICUREZZA ==========
# Secret key per JWT (se usi backend custom)
# Genera con: openssl rand -hex 32
JWT_SECRET=your_super_secret_jwt_key_here_change_me

# ========== RATE LIMITING ==========
# Richieste massime per finestra (se usi backend custom)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ========== ANALYTICS (OPZIONALE) ==========
# Google Analytics ID
GA_TRACKING_ID=

# Sentry DSN per error tracking
SENTRY_DSN=

# ========== SOCIAL / SEO ==========
# URL sito per meta tags
SITE_URL=https://piscina.example.com

# Nome sito
SITE_NAME=Sistema Gestione Piscina

# Descrizione sito
SITE_DESCRIPTION=Sistema completo per gestione piscina con prenotazioni e check-in QR code

# Meta image URL
SITE_IMAGE=https://piscina.example.com/assets/og-image.png

# ========== CONTATTI ==========
# Informazioni piscina
POOL_NAME=Piscina Comunale
POOL_ADDRESS=Via Federico Fellini, 2
POOL_CITY=Spoltore (PE)
POOL_PHONE=+39 123 456 789
POOL_EMAIL=info@piscina.it

# Coordinate Google Maps
POOL_LAT=42.4601
POOL_LNG=14.1058

# ========== NOTE ==========
# Per deployment production:
# 1. Copia questo file in .env
# 2. Sostituisci tutti i valori con quelli reali
# 3. Aggiungi .env a .gitignore (già fatto)
# 4. Configura variabili ambiente su Netlify/Vercel
# 5. Configura secrets su Supabase per Edge Functions:
#    supabase secrets set BREVO_API_KEY=xxxxx
